/**
 * @aytracker/ui — shared interface primitives.
 *
 * Currently the white-label theming layer only. Component work waits for the design reference:
 * building a component library now would mean designing AYtracker's look by accident, which is
 * exactly what the brief forbids.
 */

export * from './brand/index';
