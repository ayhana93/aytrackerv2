/**
 * Admin portal — wireframe placeholder.
 *
 * The route exists so navigation and layout can be exercised during development. Its contents
 * wait on the design reference; see docs/architecture.md § design workflow.
 */
export default function AdminPage() {
  return (
    <main>
      <h1>Admin portal</h1>
      <section className="placeholder-notice">
        <p>
          Wireframe placeholder. The domain logic and API for this portal are implemented; the
          interface is built after a design reference is provided.
        </p>
      </section>
    </main>
  );
}
